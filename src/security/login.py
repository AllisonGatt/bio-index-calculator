import models/userInformation

def createUser(userInformation: UserInformation) {
    if userInfo.email is None:
        print("Email is required")
        throw new Error
    }

def login(userLogin) {

    }

